check filesystem rootfs with path /dev/xvda1
  if space usage > 60% for 5 cycles then exec ‘/opt/bin/send_alert_message.sh'
  if space usage > 80% for 5 cycles then exec ‘/opt/bin/send_alert_message.sh'