check filesystem rootfs with path /dev/xvda1
  if space usage > 60% for 5 cycles then exec ‘/opt/bin/instance_monitor_alarm.sh'
  if space usage > 80% for 5 cycles then exec ‘/opt/bin/instance_monitor_alarm.sh'